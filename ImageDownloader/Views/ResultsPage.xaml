<UserControl x:Class="ImageDownloader.Views.ResultsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Button x:Name="GoBack"
                Style="{StaticResource MetroCircleButtonStyle}"
                Margin="10"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                ToolTip="Back to project overview">
            <Image Source="..\Images\appbar.arrow.left.png" Height="45"/>
        </Button>

        <DockPanel Margin="70,20">
            <TextBlock DockPanel.Dock="Top" Text="Images" FontSize="24"/>
            
            <ListBox x:Name="Images" Style="{StaticResource VirtualisedMetroListBox}"/>
        </DockPanel>
    </Grid>
</UserControl>
