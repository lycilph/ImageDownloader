<UserControl x:Class="ImageDownloader.Views.ImagesStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:vm="clr-namespace:ImageDownloader.ViewModels"
             xmlns:controls="clr-namespace:ImageDownloader.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance vm:ImagesStepViewModel}">
    <Grid Margin="70,20,70,0">
        <controls:FlipView cal:Message.Attach="[Event SelectionChanged] = [ItemChanged]; [Event MouseEnter] = [ShowBanner]; [Event MouseLeave] = [HideBanner]"
                           BannerText="{Binding BannerText}"
                           IsBannerEnabled="{Binding IsBannerShown}">
            <Grid>
                <TextBlock Text="Item 1"/>
            </Grid>
            <Grid>
                <TextBlock Text="Item 2"/>
            </Grid>
        </controls:FlipView>
    </Grid>
</UserControl>
