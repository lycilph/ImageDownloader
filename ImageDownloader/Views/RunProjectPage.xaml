<UserControl x:Class="ImageDownloader.Views.RunProjectPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Button x:Name="Back"
                Style="{StaticResource MetroCircleButtonStyle}"
                Margin="10"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                ToolTip="Back to project overview">
            <Image Source="..\Images\appbar.arrow.left.png" Height="45"/>
        </Button>

        <DockPanel Margin="70,20">
            <TextBlock DockPanel.Dock="Top" Text="Running project" FontSize="24"/>
            <Button x:Name="Stop" DockPanel.Dock="Top" Content="Stop"/>

            <ListBox x:Name="Log" Style="{StaticResource VirtualisedMetroListBox}"/>
        </DockPanel>
    </Grid>
</UserControl>
