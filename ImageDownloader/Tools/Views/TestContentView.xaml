<UserControl x:Class="ImageDownloader.Tools.Views.TestContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <Button x:Name="Load" Content="Load"/>
            <Button x:Name="Save" Content="Save" Margin="10,0,0,0"/>
            <Button x:Name="Home" Content="Home" Margin="10,0,0,0"/>
            <TextBox x:Name="Address" Margin="10,0,0,0"/>
        </StackPanel>
        <WebBrowser Source="{Binding Address}"/>
    </DockPanel>
</UserControl>
